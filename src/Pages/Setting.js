import React from 'react';
import Preferences from '../components/Preferences';

const Settings = () => {
  return (
    <div>
      <h1>Settings</h1>
      <Preferences />
    </div>
  );
};

export default Settings;
